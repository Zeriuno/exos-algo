/*Exercice fait par M. Jacquenod*/

procédure proc_strictement_plus_grand(par1, par2)
déclarations
  paramètres par1, par2 en réels
début
  si(par1 > par2) alors
    écrire(par1 " est plus grand que ", par2)
  sinon
    écrire(par1 " n'est pas plus grand que ", par2)
  finsi
fin

programme plus_grand
déclarations
  variables n1, n2 en réels
début
  écrire("Donnez un nombre : ")
  lire(n1)
  écrire("Donnez un nombre auquel le comparer : ")
  lire(n2)
  proc_strictement_plus_grand(n1, n2)
fin
